{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://centoui.dev/schema/globals.json",
  "title": "CentoUI Global Registry",
  "type": "object",
  "required": [ "packageDeps" ],
  "additionalProperties": false,
  "properties": {
    "$schema": {
      "type": "string",
      "description": "JSON Schema reference URI."
    },
    "packageDeps": {
      "type": "object",
      "description": "npm packages required in every CentoUI project regardless of which components are installed. The CLI installs these on first setup.",
      "additionalProperties": {
        "type": "string",
        "description": "Semver version range for the package (e.g. '^1.0.0', '~2.3.1', '>=3.0.0').",
        "pattern": "^(\\^|~|>=|>|<=|<|=)?[0-9]+\\.[0-9]+\\.[0-9]+.*$"
      }
    }
  }
}